/* ============================================================================
 *  REAL PRODUCT ICONS  (inlined as data URIs so the page stays self-contained)
 *  ---------------------------------------------------------------------------
 *  Map each service's `logo` id (see ms-data.js) to a data URI:
 *
 *      window.ICONS = {
 *        dataverse:     "data:image/svg+xml;base64,PHN2Zy...",
 *        powerapps:     "data:image/png;base64,iVBORw0K...",
 *        ...
 *      };
 *
 *  Any id present here renders the real icon; any id missing falls back to the
 *  built-in drawn mark. These are filled in from the icon files you provide.
 * ========================================================================== */
window.ICONS = {
  // dataverse:     "",
  // powerapps:     "",
  // powerautomate: "",
  // powerbi:       "",
  // powerpages:    "",
  // copilot:       "",
  // dynamics:      "",
  // sharepoint:    "",
  // teams:         "",
  // azure:         "",
};
